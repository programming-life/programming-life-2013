<%= form_for(@module_template) do |f| %>
  <% if @module_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@module_template.errors.count, "error") %> prohibited this module_template from being saved:</h2>

      <ul>
      <% @module_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
